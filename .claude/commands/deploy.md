Run the following git commands to deploy the portfolio site. Ask the user for a short commit message describing what they changed, then run:

```bash
git add index.html style.css script.js assets/
git commit -m "<their message>"
git push
```

Confirm when the push completes and remind them GitHub Pages rebuilds within ~30 seconds.
